***********************************
实验任务2：倒排索引查询
Author: 李鹏
StuID: 10175501102
Date: 2020/3/24
***********************************

python 文件名
    - project_2.py

实验环境：
    - Python 3.7
    - PyCharm

数据处理过程简述：
    - 读取倒排索引文件和查询文件，处理成合适的格式
    - 对每一个查询进行处理
        - 判断查询类型
        - 对的Term对应的DocID列表进行输出、交集、并集等操作得到结果，因为假设了docID list内部都是升序，这里采取类似归并排序的方式进行处理
    - 将结果写入输出文档

运行（假设命令行，PyCharm环境大致相同）：
    - 运行 python3 project_2.py
    - 便会得到4_generated.txt文件
    - 运行 python3 compare_query.py检查输出结果与标准结果是否相同

备注：
    - 因为考虑到docID保持升序，因为没有采取参考方案中的集合求交集、并集操作，而是采用类似归并排序的方式，得到保持升序的列表，因为最终的结果与
      提供的结果略有不同（具体python内部集合操作的实现方式还不清楚，后面再查）
    - 处理文件的时候需要注意最后一行有没有换行符，要不要输出换行符